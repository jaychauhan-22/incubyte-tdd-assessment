export class StringCalculator {
    add(numbers: string): number {
        let result = 0;

        // Check if numbers is empty
        if (numbers === "") {
            return 0;
        }

        return result;
    }
}